# Application Name
spring.application.name=backend

# Server Configuration
# Render sets the PORT environment variable dynamically (defaults to 8080 if not set)
server.port=${PORT:8080}

# Root Context Path (Ensure NO sub-paths are appended here)
server.servlet.context-path=/